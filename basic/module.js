/* CommonJs , every file is module (by default)
Modules -> Encapsulated Code (only share minimum) */
// const names = require('./names') // parameter -> where the module is located , or where the data is coming from

/* const sayHi = require('./globals-2')
console.log(names)
sayHi('susan')
sayHi(names.john)
sayHi(names.peter) */

// or
/* const data = require('./alternative')
console.log(data) */
require('./alternative-2')
